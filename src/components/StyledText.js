import React from 'react';

const StyledText = () => {
    const styles = {
        fontWeight: 400,
        fontStyle: 'normal',
        color: 'rgb(230, 192, 104)',
        fontKerning: 'none',
        textDecoration: 'none',
    };

    return (
        <span style={styles}>
            Soumen
        </span>
    );
};

export default StyledText;
